# Documents Assets

本目录保存附件和导出产物。架构图、数据流图、模块流程图和实体关系图的主来源必须是 Markdown Mermaid，位于 `.alius/workspace/docs/overview/` 或对应模块文档中。

允许保存的导出产物:

- 从 Mermaid 导出的 drawio。
- 截图。
- 外部附件。

约束:

- 不把 drawio 作为设计图主来源。
- 不再使用外部绘图文件作为 workspace 架构图维护格式。
- Markdown 文档应优先内嵌 Mermaid 图。
